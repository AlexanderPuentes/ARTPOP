<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @picture.title %>
</p>

<p>
  <strong>Url:</strong>
  <%= @picture.url %>
</p>

<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Back', pictures_path %>

<%= image_tag @pictures.image.url %>
<%= image_tag @pictures.image.url(:medium) %>
<%= image_tag @pictures.image.url(:thumb) %>
